from __future__ import annotations

# Placeholder for future business constraints (time windows, capacities, etc.).
# Keep minimal to satisfy structure and tests; wire into Service/Solver later.

def always_true(*args, **kwargs) -> bool:
    return True
